# Card images

Place card images in `pokemon/` or `onepiece/` by pack theme. Supported formats: `.avif`, `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`.

- **pokemon/** – used for Pokémon packs  
- **onepiece/** – used for One Piece packs  

Run `npm run list-cards` to regenerate the manifest after adding or removing images.
